# Repository templates

The bootstrap generator materializes `starter-skill.md.tmpl` for every initial skill. Keep templates host-neutral; host metadata belongs under `adapters/` and is applied by `tooling/plugin.py`.
